apiVersion: v1
kind: Service
metadata:
  name: {{ include "prometheustest.fullname" . }}
  labels:
    {{- include "prometheustest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prometheustest.selectorLabels" . | nindent 4 }}
